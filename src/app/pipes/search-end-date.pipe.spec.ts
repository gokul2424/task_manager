import { SearchEndDatePipe } from './search-end-date.pipe';

describe('SearchEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
