import { SearchPriorityPipe } from './search-priority.pipe';

describe('SearchPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
