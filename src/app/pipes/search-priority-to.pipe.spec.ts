import { SearchPriorityToPipe } from './search-priority-to.pipe';

describe('SearchPriorityToPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPriorityToPipe();
    expect(pipe).toBeTruthy();
  });
});
