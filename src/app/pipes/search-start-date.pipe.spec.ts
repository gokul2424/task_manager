import { SearchStartDatePipe } from './search-start-date.pipe';

describe('SearchStartDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStartDatePipe();
    expect(pipe).toBeTruthy();
  });
});
