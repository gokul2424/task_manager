import { SearchPriorityFromPipe } from './search-priority-from.pipe';

describe('SearchPriorityFromPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPriorityFromPipe();
    expect(pipe).toBeTruthy();
  });
});
